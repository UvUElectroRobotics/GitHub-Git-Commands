# Git and GitHub commands

* Commit - a snapshot of code

## New Repo
    git init
    git commit
    git remote add orgin urlToRepo
    git push -u origin master

## Existing Repo
    git remote add orgin *url to github project
    git push -u origin master

## Add, commit and push code to repo
    git add .
    git commit -m "lorem nelson"
    git push
